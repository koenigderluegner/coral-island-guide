import { IsMinimalTagBasedItemPipe } from './is-minimal-tag-based-item.pipe';

describe('IsMinimalTagBasedItemPipe', () => {
    it('create an instance', () => {
        const pipe = new IsMinimalTagBasedItemPipe();
        expect(pipe).toBeTruthy();
    });
});
