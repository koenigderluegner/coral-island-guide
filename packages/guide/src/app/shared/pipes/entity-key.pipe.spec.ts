import { EntityKeyPipe } from './entity-key.pipe';

describe('EntityKeyPipe', () => {
    it('create an instance', () => {
        const pipe = new EntityKeyPipe();
        expect(pipe).toBeTruthy();
    });
});
