import { IsBaseCropPipe } from './is-base-crop.pipe';

describe('IsBaseCropPipe', () => {
    it('create an instance', () => {
        const pipe = new IsBaseCropPipe();
        expect(pipe).toBeTruthy();
    });
});
