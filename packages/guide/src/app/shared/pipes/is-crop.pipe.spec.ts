import { IsCropPipe } from './is-crop.pipe';

describe('IsCropPipe', () => {
    it('create an instance', () => {
        const pipe = new IsCropPipe();
        expect(pipe).toBeTruthy();
    });
});
