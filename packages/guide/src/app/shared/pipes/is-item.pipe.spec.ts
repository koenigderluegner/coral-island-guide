import { IsItemPipe } from './is-item.pipe';

describe('IsItemPipe', () => {
    it('create an instance', () => {
        const pipe = new IsItemPipe();
        expect(pipe).toBeTruthy();
    });
});
