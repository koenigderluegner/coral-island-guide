import { RemoveTagsPipe } from './remove-tags.pipe';

describe('RemoveTagsPipe', () => {
    it('create an instance', () => {
        const pipe = new RemoveTagsPipe();
        expect(pipe).toBeTruthy();
    });
});
