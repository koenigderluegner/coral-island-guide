import { IsMinimalItemPipe } from './is-minimal-item.pipe';

describe('IsMinimalItemPipe', () => {
    it('create an instance', () => {
        const pipe = new IsMinimalItemPipe();
        expect(pipe).toBeTruthy();
    });
});
