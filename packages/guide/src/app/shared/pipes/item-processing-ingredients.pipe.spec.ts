import { ItemProcessingIngredientsPipe } from './item-processing-ingredients.pipe';

describe('ItemProcessingIngredientsPipe', () => {
    it('create an instance', () => {
        const pipe = new ItemProcessingIngredientsPipe();
        expect(pipe).toBeTruthy();
    });
});
