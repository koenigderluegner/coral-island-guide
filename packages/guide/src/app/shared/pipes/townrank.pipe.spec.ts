import { TownrankPipe } from './townrank.pipe';

describe('TownrankPipe', () => {
    it('create an instance', () => {
        const pipe = new TownrankPipe();
        expect(pipe).toBeTruthy();
    });
});
