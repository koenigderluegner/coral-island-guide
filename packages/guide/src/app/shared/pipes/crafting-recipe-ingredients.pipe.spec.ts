import { CraftingRecipeIngredientsPipe } from './crafting-recipe-ingredients.pipe';

describe('CraftingRecipeIngredientsPipe', () => {
    it('create an instance', () => {
        const pipe = new CraftingRecipeIngredientsPipe();
        expect(pipe).toBeTruthy();
    });
});
