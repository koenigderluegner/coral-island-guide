import { AddSpacesToPascalCasePipe } from './add-spaces-to-pascal-case.pipe';

describe('AddSpacesToPascalCasePipe', () => {
    it('create an instance', () => {
        const pipe = new AddSpacesToPascalCasePipe();
        expect(pipe).toBeTruthy();
    });
});
