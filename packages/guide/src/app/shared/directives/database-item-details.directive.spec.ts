import { DatabaseItemDetailsDirective } from './database-item-details.directive';

describe('DatabaseItemDetailsDirective', () => {
    it('should create an instance', () => {
        const directive = new DatabaseItemDetailsDirective();
        expect(directive).toBeTruthy();
    });
});
